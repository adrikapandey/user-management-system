export const ROLES = {
  ADMIN: "admin",
  MANAGER: "manager",
  USER: "user"
};

export const USER_STATUSES = {
  ACTIVE: "active",
  INACTIVE: "inactive"
};

export const ROLE_OPTIONS = Object.values(ROLES);
export const STATUS_OPTIONS = Object.values(USER_STATUSES);
